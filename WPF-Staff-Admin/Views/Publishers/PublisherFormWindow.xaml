<Window x:Class="WPF_Staff_Admin.Views.Publishers.PublisherFormWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{Binding Title}"
        Height="350" Width="500"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <Border Grid.Row="0"
                Background="White"
                BorderBrush="#E0E0E0"
                BorderThickness="0,0,0,1"
                Padding="20">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="🏢"
                          FontSize="24"
                          VerticalAlignment="Center"
                          Margin="0,0,10,0"/>
                <TextBlock Text="{Binding Title}"
                          FontSize="20"
                          FontWeight="Bold"
                          Foreground="#263238"
                          VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Background="White" Padding="20">
            <StackPanel>
                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="130"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0"
                              Text="Tên nhà xuất bản *"
                              FontSize="14"
                              VerticalAlignment="Center"/>

                    <Border Grid.Column="1"
                           BorderBrush="#BDBDBD"
                           BorderThickness="1"
                           CornerRadius="3">
                        <TextBox Text="{Binding PublisherName, UpdateSourceTrigger=PropertyChanged}"
                                Padding="10"
                                FontSize="14"
                                BorderThickness="0"/>
                    </Border>
                </Grid>

                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="130"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0"
                              Text="Website"
                              FontSize="14"
                              VerticalAlignment="Center"/>

                    <Border Grid.Column="1"
                           BorderBrush="#BDBDBD"
                           BorderThickness="1"
                           CornerRadius="3">
                        <TextBox Text="{Binding Web, UpdateSourceTrigger=PropertyChanged}"
                                Padding="10"
                                FontSize="14"
                                BorderThickness="0"/>
                    </Border>
                </Grid>
            </StackPanel>
        </Border>

        <Border Grid.Row="2"
                Background="White"
                BorderBrush="#E0E0E0"
                BorderThickness="0,1,0,0"
                Padding="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                          VerticalAlignment="Center"
                          FontSize="12"
                          Foreground="#757575">
                    <Run Text="*"/>
                    <Run Text="Trường bắt buộc"/>
                </TextBlock>

                <StackPanel Grid.Column="1"
                           Orientation="Horizontal">
                    <Button Content="Hủy"
                           Command="{Binding CancelCommand}"
                           Background="#757575"
                           Foreground="White"
                           BorderThickness="0"
                           Padding="25,10"
                           FontSize="14"
                           Margin="0,0,10,0"/>

                    <Button Content="💾 Lưu"
                           Command="{Binding SaveCommand}"
                           Background="#4CAF50"
                           Foreground="White"
                           BorderThickness="0"
                           Padding="30,10"
                           FontSize="14"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="0" Grid.RowSpan="3"
               Background="White"
               Opacity="0.9"
               Visibility="{Binding IsSaving, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel VerticalAlignment="Center"
                       HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True"
                            Width="200"
                            Height="4"
                            Margin="0,0,0,10"/>
                <TextBlock Text="Đang lưu..."
                          FontSize="14"
                          HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
