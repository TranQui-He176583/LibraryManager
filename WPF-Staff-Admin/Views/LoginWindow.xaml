<Window x:Class="WPF_Staff_Admin.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_Staff_Admin.Views"
        mc:Ignorable="d"
        Title="Đăng nhập - Library Management System"
        Height="500" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F5F5F5">
    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#2196F3" Offset="0"/>
                <GradientStop Color="#1976D2" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <Border Width="350" Height="480"
                Background="White"
                CornerRadius="10"
                VerticalAlignment="Center"
                HorizontalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect Color="Black" Opacity="0.3" BlurRadius="20" ShadowDepth="5"/>
            </Border.Effect>

            <StackPanel Margin="30">
                <Viewbox Width="80" Height="80" Margin="0,0,0,20">
                    <Grid Width="100" Height="100">
                        <Ellipse Fill="#2196F3" Width="100" Height="100"/>
                        <TextBlock Text="📚" 
                                  FontSize="50"
                                  HorizontalAlignment="Center"
                                  VerticalAlignment="Center"/>
                    </Grid>
                </Viewbox>

                <TextBlock Text="Library Management"
                          FontSize="24"
                          FontWeight="Bold"
                          Foreground="#333"
                          HorizontalAlignment="Center"
                          Margin="0,0,0,5"/>

                <TextBlock Text="System Login"
                          FontSize="14"
                          Foreground="#666"
                          HorizontalAlignment="Center"
                          Margin="0,0,0,30"/>

                <TextBlock Text="Username"
                          FontSize="13"
                          Foreground="#666"
                          Margin="0,0,0,5"/>

                <Border BorderBrush="#DDD"
                       BorderThickness="1"
                       CornerRadius="5"
                       Margin="0,0,0,15">
                    <TextBox x:Name="UsernameTextBox"
                            Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                            FontSize="14"
                            Padding="10"
                            BorderThickness="0"
                            Background="Transparent">
                        <TextBox.InputBindings>
                            <KeyBinding Key="Enter" Command="{Binding LoginCommand}"/>
                        </TextBox.InputBindings>
                    </TextBox>
                </Border>

                <TextBlock Text="Password"
                          FontSize="13"
                          Foreground="#666"
                          Margin="0,0,0,5"/>

                <Border BorderBrush="#DDD"
                       BorderThickness="1"
                       CornerRadius="5"
                       Margin="0,0,0,10">
                    <PasswordBox x:Name="PasswordBox"
                                FontSize="14"
                                Padding="10"
                                BorderThickness="0"
                                Background="Transparent"
                                PasswordChanged="PasswordBox_PasswordChanged">
                        <PasswordBox.InputBindings>
                            <KeyBinding Key="Enter" Command="{Binding LoginCommand}"/>
                        </PasswordBox.InputBindings>
                    </PasswordBox>
                </Border>

                <TextBlock Text="{Binding ErrorMessage}"
                          FontSize="12"
                          Foreground="Red"
                          TextWrapping="Wrap"
                          Margin="0,5,0,15"
                          MinHeight="20"
                          HorizontalAlignment="Center"
                          TextAlignment="Center"/>

                <Button Content="ĐĂNG NHẬP"
                       Command="{Binding LoginCommand}"
                       Height="45"
                       FontSize="14"
                       FontWeight="Bold"
                       Foreground="White"
                       Background="#2196F3"
                       BorderThickness="0"
                       Cursor="Hand"
                       Margin="0,0,0,10">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="border"
                                               Background="{TemplateBinding Background}"
                                               CornerRadius="5"
                                               BorderThickness="0">
                                            <ContentPresenter HorizontalAlignment="Center"
                                                            VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="#1976D2"/>
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="#1565C0"/>
                                            </Trigger>
                                            <Trigger Property="IsEnabled" Value="False">
                                                <Setter TargetName="border" Property="Background" Value="#BBDEFB"/>
                                                <Setter Property="Cursor" Value="Wait"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>

                <StackPanel Orientation="Horizontal"
                           HorizontalAlignment="Center"
                           Margin="0,10,0,0"
                           Height="20">
                    <StackPanel.Style>
                        <Style TargetType="StackPanel">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Style>

                    <TextBlock Text="Đang đăng nhập..."
                              FontSize="12"
                              Foreground="#666"
                              VerticalAlignment="Center"
                              Margin="0,0,10,0"/>
                    <ProgressBar IsIndeterminate="True"
                                Width="100"
                                Height="3"
                                VerticalAlignment="Center"/>
                </StackPanel>

                <Border Background="#FFF3E0"
                       BorderBrush="#FFB74D"
                       BorderThickness="1"
                       CornerRadius="5"
                       Padding="10"
                       Margin="0,20,0,0">
                    <StackPanel>
                        <TextBlock Text="🔑 Tài khoản test:"
                                  FontSize="11"
                                  FontWeight="Bold"
                                  Foreground="#E65100"
                                  Margin="0,0,0,5"/>
                        <TextBlock FontSize="10" Foreground="#424242">
                            <Run Text="Admin: admin / admin123" FontWeight="SemiBold"/>
                            <LineBreak/>
                            <Run Text="Staff: staff / staff123" FontWeight="SemiBold"/>
                        </TextBlock>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Border>
    </Grid>
</Window>
