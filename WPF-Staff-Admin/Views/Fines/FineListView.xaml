<UserControl x:Class="WPF_Staff_Admin.Views.Fines.FineListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF_Staff_Admin.Views.Fines"
             xmlns:viewmodels="clr-namespace:WPF_Staff_Admin.ViewModels.Fines"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000"
             Background="White">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel Grid.Row="0" LastChildFill="False" Margin="0,0,0,20">
            <StackPanel DockPanel.Dock="Left">
                <TextBlock Text="Danh sách Tiền phạt" FontSize="28" FontWeight="SemiBold" Foreground="#2C3E50"/>
                <TextBlock Text="Quản lý các khoản phạt quá hạn, mất hoặc hỏng sách" Foreground="Gray" FontSize="14" Margin="0,5,0,0"/>
            </StackPanel>

            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="🔄 Làm mới" Command="{Binding RefreshCommand}" Style="{StaticResource ModernButtonStyle}" Background="#3498DB" Foreground="White" Width="120" Height="40" Margin="10,0,0,0"/>
                <Button Content="💰 Thu tiền" Command="{Binding PayFineCommand}" Style="{StaticResource ModernButtonStyle}" Background="#2ECC71" Foreground="White" Width="120" Height="40" Margin="10,0,0,0"/>
            </StackPanel>
        </DockPanel>

        <!-- Filters -->
        <Border Grid.Row="1" Background="#F8F9FA" CornerRadius="8" Padding="15" Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal">
                <StackPanel Margin="0,0,20,0">
                    <TextBlock Text="Tìm kiếm" FontWeight="SemiBold" Margin="0,0,0,5" FontSize="12"/>
                    <TextBox Width="300" Height="35" Padding="10,5" VerticalContentAlignment="Center"
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                             Style="{StaticResource SearchTextBoxStyle}"
                             Tag="Tìm theo tên thành viên, tên sách..."/>
                </StackPanel>

                <StackPanel Margin="0,0,20,0">
                    <TextBlock Text="Trạng thái" FontWeight="SemiBold" Margin="0,0,0,5" FontSize="12"/>
                    <ComboBox Width="200" Height="35" 
                              ItemsSource="{Binding StatusFilters}" 
                              SelectedItem="{Binding SelectedStatusFilter}"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Data Grid -->
        <Border Grid.Row="2" Background="White" CornerRadius="8" BorderBrush="#E0E0E0" BorderThickness="1">
            <Grid>
                <DataGrid ItemsSource="{Binding Fines}" 
                          SelectedItem="{Binding SelectedFine}"
                          AutoGenerateColumns="False" 
                          CanUserAddRows="False" 
                          IsReadOnly="True"
                          RowHeight="45"
                          HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal"
                          VerticalAlignment="Stretch"
                          HorizontalAlignment="Stretch"
                          BorderThickness="0"
                          Background="Transparent"
                          SelectionMode="Single"
                          RowHeaderWidth="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Thành viên" Binding="{Binding MemberName}" Width="1.5*">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="Padding" Value="10,0"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Tên sách" Binding="{Binding BookTitle}" Width="2*">
                             <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="Padding" Value="10,0"/>
                                    <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Lý do" Binding="{Binding Reason}" Width="1.5*">
                             <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="Padding" Value="10,0"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Số tiền" Binding="{Binding AmountDisplay}" Width="*">
                             <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="Padding" Value="10,0"/>
                                    <Setter Property="FontWeight" Value="SemiBold" />
                                    <Setter Property="Foreground" Value="#E74C3C"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTemplateColumn Header="Trạng thái" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="12" Padding="8,2" HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsPaid}" Value="True">
                                                        <Setter Property="Background" Value="#D4EFDF"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding IsPaid}" Value="False">
                                                        <Setter Property="Background" Value="#FADBD8"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Text="{Binding StatusDisplay}" FontSize="12" FontWeight="SemiBold">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsPaid}" Value="True">
                                                            <Setter Property="Foreground" Value="#1E8449"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding IsPaid}" Value="False">
                                                            <Setter Property="Foreground" Value="#CB4335"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTextColumn Header="Ngày tạo" Binding="{Binding CreatedAtDisplay}" Width="*">
                             <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="Padding" Value="10,0"/>
                                    <Setter Property="FontSize" Value="12"/>
                                    <Setter Property="Foreground" Value="Gray"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Loading Overlay -->
                <Border Background="#80FFFFFF" Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <ProgressBar IsIndeterminate="True" Width="200" Height="10"/>
                        <TextBlock Text="Đang xử lý..." Margin="0,10,0,0" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
                
                <!-- Empty State -->
                <TextBlock Text="Không tìm thấy bản ghi nào" 
                           Visibility="{Binding Fines.Count, Converter={StaticResource ZeroToVisibilityConverter}}"
                           VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="18" Foreground="Gray"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
